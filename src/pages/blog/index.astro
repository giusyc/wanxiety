---
import { getCollection } from 'astro:content';
import Layout from '@layouts/Default.astro';
import BlogList from '@components/blog/BlogList.astro';
import CareerPaths from '@components/careerPaths/CareerPaths.astro';

const posts = await getCollection('blog').then((collection) =>
  collection.reverse()
);
const careers = await getCollection('careerPaths');
---

<Layout
  title='Blog'
  description='Brutal Blog | On this page you can find a collection of blogposts'
  pageTitle='Brutal Blog'
>
  <main class='bg-green p-6'>
    <BlogList posts={posts} />
    <CareerPaths careers={careers} />
  </main>
</Layout>
